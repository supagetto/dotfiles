#!/usr/bin/env zsh

# ┏━━━━━━━━━┓
# ┃ aliases ┃
# ┗━━━━━━━━━┛

__zsh::config::aliases::misc() {
  alias vim="nvim"
  alias rm="trash"
  alias ls="lsd"
}

__zsh::config::aliases::general() {
  alias mkdir="mkdir -p"
}

() {
  __zsh::config::aliases::general
  __zsh::config::aliases::misc
}
